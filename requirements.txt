# ──────────────────────────────────────────────────────────────────────────────
# TEMGen requirements
#
# PyTorch + CUDA: install via Perlmutter module or conda, NOT pip.
#   module load pytorch/2.1.0-cu12
#   OR: conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
#
# PyG: install after PyTorch is set up.
#   pip install torch-geometric torch-scatter torch-sparse
#
# Everything below can be pip installed directly.
# ──────────────────────────────────────────────────────────────────────────────

# Config
omegaconf>=2.3.0

# Data
h5py>=3.10.0
zarr>=2.16.0
numpy>=1.24.0
pymatgen>=2023.11.0
tqdm>=4.66.0

# Training
pytorch-lightning>=2.1.0
tensorboard>=2.15.0